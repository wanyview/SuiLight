# SuiLight 平台化 - SaaS 架构设计

## 一、平台化目标

### 从产品到平台

```
当前状态 (产品):
└── SuiLight 知识沙龙
    └── 预定义的 100 位思想家
    └── 自然科学/社会科学等固定领域
    └── 单一用途

目标状态 (平台):
└── SuiLight Platform
    ├── 核心框架 (领域无关)
    │   ├── Agent 引擎
    │   ├── 讨论系统
    │   ├── 知识涌现
    │   └── 任务队列
    │
    ├── 可配置组件
    │   ├── 领域模板 (可自定义)
    │   ├── Agent 预设 (可导入/导出)
    │   ├── 讨论流程 (可编排)
    │   └── DATM 模型 (可定制)
    │
    ├── 多租户支持
    │   ├── 租户隔离
    │   ├── 配额管理
    │   └── 数据隔离
    │
    └── 应用市场
        ├── 领域模板市场
        ├── Agent 市场
        └── API 市场
```

---

## 二、核心架构设计

### 🏗️ 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                      应用层 (SaaS)                           │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ 教育领域 │  │ 科研领域 │  │ 企业领域 │  │ 医疗领域 │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
├─────────────────────────────────────────────────────────────┤
│                    配置层 (Config)                           │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ 领域配置 │  │ 流程配置 │  │ Agent配置│  │ DATM配置 │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
├─────────────────────────────────────────────────────────────┤
│                    核心层 (Core)                             │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Agent Engine                                       │   │
│  │  ├── Agent Registry                                 │   │
│  │  ├── LLM Interface                                  │   │
│  │  └── DATM Calculator                                │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Discussion Manager                                 │   │
│  │  ├── Phase Controller                               │   │
│  │  ├── Contribution Tracker                           │   │
│  │  └── Insight Generator                              │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Knowledge Engine                                   │   │
│  │  ├── Parser                                         │   │
│  │  ├── Graph                                          │   │
│  │  └── Generator                                      │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                    基础设施层 (Infra)                        │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │  多租户  │  │  存储    │  │  队列    │  │  API    │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、可配置组件设计

### 1️⃣ 领域模板 (Domain Template)

```python
# 领域配置示例
domain_template = {
    "id": "custom_domain",
    "name": "自定义领域",
    "description": "用户自定义的知识沙龙领域",
    
    # DATM 权重配置
    "datm_weights": {
        "truth": 0.3,        # 科学性权重
        "goodness": 0.3,     # 社科性权重
        "beauty": 0.2,       # 人文性权重
        "intelligence": 0.2  # 创新性权重
    },
    
    # 评估维度
    "evaluation_dims": ["创新度", "可行性", "成本效益"],
    
    # 讨论阶段
    "phases": [
        {"name": "引言", "duration": 10},
        {"name": "分析", "duration": 30},
        {"name": "辩论", "duration": 45},
        {"name": "综合", "duration": 15}
    ],
    
    # 默认 Agent 类型
    "agent_types": [
        {"role": "主讲人", "count": 1},
        {"role": "评论员", "count": 3},
        {"role": "批评者", "count": 2}
    ]
}
```

### 2️⃣ Agent 模板 (Agent Template)

```python
# Agent 配置示例
agent_template = {
    "id": "custom_agent_001",
    "name": "领域专家",
    "type": "custom",  # preset / custom / ai_generated
    
    # DATM 初始值
    "datm": {
        "truth": 70,
        "goodness": 60,
        "beauty": 50,
        "intelligence": 75
    },
    
    # 专长领域
    "expertise": ["领域知识", "方法论", "案例分析"],
    
    # 性格配置
    "personality": {
        "style": "professional",  # professional / casual / academic
        "verbosity": "medium",    # short / medium / long
        "openness": 0.7           # 0-1, 接受新观点的程度
    },
    
    # 知识来源
    "knowledge_sources": [
        {"type": "document", "path": "/docs/domain_knowledge.md"},
        {"type": "url", "url": "https://example.com"},
        {"type": "api", "endpoint": "/api/knowledge"}
    ]
}
```

### 3️⃣ 讨论流程配置 (Workflow Config)

```python
# 讨论流程配置
workflow_config = {
    "id": "custom_workflow",
    "name": "自定义讨论流程",
    
    "stages": [
        {
            "name": "问题定义",
            "type": "introduction",
            "duration_min": 10,
            "participants": ["all"],
            "output_format": "summary"
        },
        {
            "name": "多角度分析",
            "type": "perspectives",
            "duration_min": 30,
            "participants": ["expert_1", "expert_2", "expert_3"],
            "output_format": "analysis_report"
        },
        {
            "name": "观点辩论",
            "type": "debate",
            "duration_min": 45,
            "participants": ["proponent", "opponent", "moderator"],
            "output_format": "debate_record"
        },
        {
            "name": "综合结论",
            "type": "synthesis",
            "duration_min": 15,
            "participants": ["synthesizer"],
            "output_format": "conclusion"
        }
    ],
    
    # 规则配置
    "rules": {
        "allow_repeat_speakers": True,
        "max_contributions_per_agent": 5,
        "enable_voting": True,
        "require_consensus": False
    },
    
    # 输出配置
    "outputs": {
        "insights": True,
        "action_items": True,
        "knowledge_graph": True,
        "summary_report": True
    }
}
```

### 4️⃣ DATM 模型定制

```python
# 自定义 DATM 模型
custom_datm = {
    "name": "四维标准",
    "dimensions": [
        {
            "id": "truth",
            "name": "Truth (真)",
            "description": "科学性、客观性、证据支撑",
            "metrics": ["数据支撑度", "逻辑严密性", "可重复性"]
        },
        {
            "id": "goodness",
            "name": "Goodness (善)",
            "description": "社科性、价值观、伦理考量",
            "metrics": ["价值观正向性", "伦理合规性", "社会责任"]
        },
        {
            "id": "beauty",
            "name": "Beauty (美)",
            "description": "人文性、美学、表达方式",
            "metrics": ["表达清晰度", "审美价值", "情感共鸣"]
        },
        {
            "id": "intelligence",
            "name": "Intelligence (灵)",
            "description": "创新性、洞察力、前沿性",
            "metrics": ["创新程度", "洞察深度", "前瞻性"]
        }
    ],
    
    # 计算方法
    "calculation": {
        "method": "weighted_average",
        "weights": [0.25, 0.25, 0.25, 0.25],
        "normalization": "min_max"
    }
}
```

---

## 四、多租户设计

### 🏢 租户模型

```
Tenant (租户)
├── tenant_id: string (唯一标识)
├── name: string (租户名称)
├── plan: string (free/pro/enterprise)
├── quota: dict (配额)
│   ├── agents: 100
│   ├── discussions: 50
│   ├── storage_gb: 1
│   └── api_calls: 10000/month
├── domains: list (拥有的领域)
├── users: list (用户列表)
└── billing: dict (账单信息)
```

### 🔐 权限模型

```
Role (角色)
├── admin (租户管理员)
│   ├── 管理租户设置
│   ├── 管理用户
│   └── 管理所有资源
│
├── editor (编辑者)
│   ├── 创建/编辑领域
│   ├── 创建/编辑 Agent
│   └── 管理讨论
│
├── viewer (查看者)
│   ├── 查看领域
│   ├── 查看 Agent
│   └── 参与讨论
│
└── api_user (API 用户)
    └── API 访问权限
```

---

## 五、实施路径

### Phase 1: 核心抽象 (1-2周)

| 任务 | 输出 |
|------|------|
| 抽象 Agent 引擎 | 可配置的 Agent Registry |
| 抽象讨论系统 | 可配置的 Workflow Engine |
| 抽象 DATM | 可配置的 Evaluation Model |
| 抽象存储层 | 多租户 Storage Adapter |

### Phase 2: 配置系统 (1周)

| 任务 | 输出 |
|------|------|
| 领域配置 DSL | JSON/YAML 配置格式 |
| Agent 模板系统 | 可导入/导出的模板 |
| 工作流设计器 | 可视化流程配置 UI |
| 配置验证器 | 配置语法检查 |

### Phase 3: 多租户 (1周)

| 任务 | 输出 |
|------|------|
| 租户管理 | 租户 CRUD API |
| 配额系统 | 用量统计 + 限制 |
| 权限系统 | RBAC 实现 |
| 计费系统 | 账单管理 |

### Phase 4: 市场功能 (1周)

| 任务 | 输出 |
|------|------|
| 模板市场 | 领域/Agent 分享市场 |
| 导入导出 | 配置分享功能 |
| 评分系统 | 模板评分 + 评论 |
| API 市场 | API 开放平台 |

---

## 六、平台 vs 当前产品

### 对比表

| 维度 | 当前产品 | 平台化后 |
|------|----------|---------|
| **目标用户** | 单一场景 | 多元化 |
| **领域** | 固定 (知识沙龙) | 任意领域 |
| **Agent** | 预设 100 位 | 可自定义/导入 |
| **流程** | 固定流程 | 可编排 |
| **部署** | 单体部署 | SaaS / 私有化 |
| **商业模式** | 未验证 | 订阅制 |
| **扩展性** | 低 | 高 |
| **开发难度** | 低 | 中 |
| **维护成本** | 低 | 中-高 |

---

## 七、商业模式

### 💰 定价策略

| 计划 | 价格 | 功能 |
|------|------|------|
| **Free** | ¥0 | 5 Agents, 10 Discussions, 基础功能 |
| **Pro** | ¥99/月 | 100 Agents, 100 Discussions, 全部功能 |
| **Enterprise** | ¥999/月 | 无限, 私有化部署, 专属支持 |

### 📈 收入预测

```
第一年:
├── Free 用户: 1000
├── Pro 用户: 50 (¥99×12×50 = ¥59,400)
├── Enterprise: 5 (¥999×12×5 = ¥59,940)
└── 总收入: ~¥120,000

第二年:
├── Free 用户: 5000
├── Pro 用户: 200
├── Enterprise: 20
└── 总收入: ~¥500,000
```

---

## 八、技术债务

### ⚠️ 需要重构的部分

| 组件 | 当前问题 | 重构方案 |
|------|---------|---------|
| `src/agents/base.py` | 硬编码 DATM | 配置文件驱动 |
| `src/knowledge/discussion.py` | 固定流程 | 工作流引擎 |
| `src/agents/presets.py` | 预设数据 | 可配置的模板系统 |
| `src/main.py` | 单体架构 | 微服务拆分 |

---

## 九、下一步行动

### 🎯 立即开始

1. **重构 Agent 引擎** - 使 DATM 可配置
2. **设计配置格式** - JSON Schema
3. **创建管理 API** - 领域/Agent/Workflow CRUD
4. **开发导入导出** - 配置分享功能

### 📋 任务清单

```markdown
## Week 1: 核心抽象
- [ ] 抽象 Agent Registry
- [ ] 抽象 Workflow Engine
- [ ] 设计配置 Schema
- [ ] 实现配置验证

## Week 2: 配置系统
- [ ] 领域配置 API
- [ ] Agent 模板 API
- [ ] 工作流配置 API
- [ ] 配置导入导出

## Week 3: 多租户
- [ ] 租户管理
- [ ] 配额系统
- [ ] 权限控制
- [ ] 计费基础

## Week 4: 平台上线
- [ ] 文档完善
- [ ] 部署上线
- [ ] 市场功能
- [ ] 用户引导
```

---

## 十、总结

### 平台化价值

| 价值 | 说明 |
|------|------|
| **可扩展性** | 从单一场景到全行业 |
| **可复制性** | 快速复制到新领域 |
| **可变现** | 订阅制 + 企业版 |
| **可生态** | 模板市场 + API 市场 |

### 核心指标

| 指标 | 目标 |
|------|------|
| **上线时间** | 4周 |
| **开发成本** | ~¥10,000 |
| **预期用户** | 第1月 100, 第1年 1000 |
| **预期收入** | 第1年 ¥120,000 |

---

**需要我开始实施吗？我可以从 "核心抽象" 开始。**
